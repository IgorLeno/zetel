# Zetel migration

No bootstrap:

- nenhum arquivo em `app/`, `components/`, `lib/`, `migrations/` ou dados foi
  alterado;
- `AGENTS.md` e `CLAUDE.md` permanecem inalterados;
- somente spec, estado e relatorios do piloto entram na branch.

Migracao planejada:

1. fundacao e state machine;
2. lifecycle de spec/tarefa;
3. reviews e fronteira de sessao;
4. skills locais;
5. adaptadores curtos com inventario de preservacao;
6. convergencia e avaliacao.

Nao ha autorizacao para aplicar o workflow a outros repositorios.
